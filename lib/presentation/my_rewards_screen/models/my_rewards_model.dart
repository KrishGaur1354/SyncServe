import 'userprofile_item_model.dart';
import '../../../core/app_export.dart';

class MyRewardsModel {
  List<UserprofileItemModel> userprofileItemList =
      List.generate(3, (index) => UserprofileItemModel());
}
